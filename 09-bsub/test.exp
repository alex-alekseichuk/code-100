#!/usr/bin/env -S expect -f

source [file join [file dirname [info script]] .. common common.exp]

expect_strict "Введите элементы: " "waiting for prompt"
send_clean "a b c\r"
expect_strict "a \r\nb \r\na b \r\nc \r\na c \r\nb c" "waiting for result"
